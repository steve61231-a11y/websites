# Copy to .env.local and fill in from your Supabase project
# (Supabase dashboard -> Project Settings -> API).
#
# Leave these blank to run the app in demo mode: everything works, but the data
# lives only in your browser and nothing touches the school's real records.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
